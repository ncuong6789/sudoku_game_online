import React from 'react';

export default function ChessGame() {
    return (
        <div className="glass-panel menu-container">
            <h2>Chess Game (Đang phát triển)</h2>
            <p style={{ color: 'var(--text-secondary)' }}>Phần này sẽ được triển khai ở bước tiếp theo.</p>
        </div>
    );
}
